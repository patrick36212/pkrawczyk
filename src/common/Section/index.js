import React from "react";
import { PageSection } from "./styled";

const Section = ({ content }) => {
    return (
        <PageSection>
            {content}
        </PageSection>
    )
};

export default Section;